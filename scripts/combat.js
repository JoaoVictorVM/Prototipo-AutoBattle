// Loop de combate. NÃO toca no DOM — apenas atualiza o estado.
// Implementado em fases posteriores.

import { state } from "./state.js";

export function combatTick(_dt) {
  // Fase 2 — implementar IA, movimento, ataque, morte.
  void state;
}
